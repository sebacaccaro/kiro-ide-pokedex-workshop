// Setup globale per i test dei componenti.
// Importa i matcher di jest-dom (es. toBeInTheDocument, toHaveAttribute)
// registrandoli su `expect` per tutti i test.
import '@testing-library/jest-dom/vitest';
